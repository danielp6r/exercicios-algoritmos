algoritmo "47"
var
   x, contador: inteiro
inicio
      x <- 500
      contador <- x -50
      enquanto (contador >= 0) faca
               x <- x + contador
               contador <- contador -50
      fimenquanto
      
      escreval(x)

fimalgoritmo