algoritmo "90"
var
   x, y: inteiro
procedimento somador(a,b: inteiro)
inicio
   x <- a
   y <- b
   escreva("A soma de", a, " e", b, " é igual a:", a+b)
fimprocedimento
inicio
      somador(12,7)
fimalgoritmo