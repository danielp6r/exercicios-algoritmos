algoritmo "dobroTerca"
var
   n, dobro, terca: real

inicio
      escreval("Digite um número:")
      leia(n)
      dobro <- n * 2
      terca <- n / 3
      escreval("O dobro de", n, " é", dobro)
      escreval("A terça parte de", n, " é", terca)

fimalgoritmo