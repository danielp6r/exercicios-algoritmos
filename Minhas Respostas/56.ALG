algoritmo "56"
var
   numero, soma: real
inicio
      enquanto (numero <> 1111) faca
               escreval("Digite um número:")
               leia(numero)
               soma <- soma + numero
      fimenquanto
      
      escreval("A soma de todos os números é:", soma)

fimalgoritmo